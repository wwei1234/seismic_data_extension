对目前最新方案的修改：

1. 样本生成

子波：由井反射系数和井旁地震道做频率域反褶积估计得到，这个保持不变
但是不进行整形，直接由估计子波和反射系数进行褶积得到我们的宽频标签记录，然后对宽频记录进行滤波得到窄频带记录样本

归一化的时候统一使用百分比位方法，训练时用窄频带样本的作为标准，推理时由网络输入作为标准

低频去除模块关闭

切片大小不变，步长改为128

数据增强把上下翻转去掉，只需要左右翻转

构造反射系数剖面的时候断层不要垂直断层，要有角度的断层，如果加断层的话角度就从75、60、45、30这几个数据里随机挑选

